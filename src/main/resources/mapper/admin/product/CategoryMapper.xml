<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.admin.product.CategoryMapper">

    <!-- 카테고리 전체 목록 조회 (연결 상품 수 포함) -->
    <select id="findAll" resultType="org.example.board_cafe_kiosk_2603.dto.admin.product.CategoryResponseDTO">
        SELECT c.id,
               c.name,
               c.type,
               CASE
                   WHEN c.type = 'GAME' THEN
                           (SELECT COUNT(*) FROM game g WHERE g.category_id = c.id)
                   ELSE
                       (SELECT COUNT(*) FROM menu m WHERE m.category_id = c.id AND m.is_deleted = false)
                   END AS productCount
        FROM category c
        ORDER BY c.type, c.id
    </select>

    <!-- type 기준 카테고리 목록 조회 (연결 상품 수 포함) -->
    <select id="findByType" resultType="org.example.board_cafe_kiosk_2603.dto.admin.product.CategoryResponseDTO">
        SELECT
            c.id,
            c.name,
            c.type,
            COUNT(m.id) AS productCount
        FROM category c
                 LEFT JOIN menu m ON m.category_id = c.id AND m.is_deleted = false
        WHERE c.type = #{type}
        GROUP BY c.id, c.name, c.type
        ORDER BY c.id
    </select>

    <!-- PK로 카테고리 단건 조회 -->
    <select id="findById" resultType="org.example.board_cafe_kiosk_2603.dto.admin.product.CategoryResponseDTO">
        SELECT
            c.id,
            c.name,
            c.type,
            COUNT(m.id) AS productCount
        FROM category c
                 LEFT JOIN menu m ON m.category_id = c.id AND m.is_deleted = false
        WHERE c.id = #{id}
        GROUP BY c.id, c.name, c.type
    </select>

    <!-- 카테고리 등록 -->
    <insert id="insert"
            parameterType="org.example.board_cafe_kiosk_2603.domain.admin.product.Category"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO category (name, type)
        VALUES (#{name}, #{type})
    </insert>

    <!-- 카테고리 수정 -->
    <update id="update">
        UPDATE category
        SET
            name = #{dto.name},
            type = #{dto.type}
        WHERE id = #{id}
    </update>

    <!-- 카테고리 삭제 -->
    <delete id="delete">
        DELETE FROM category
        WHERE id = #{id}
    </delete>

    <!-- 카테고리에 연결된 상품 수 조회 -->
    <select id="countLinkedProducts" resultType="int">
        SELECT COUNT(*)
        FROM menu
        WHERE category_id = #{id}
          AND is_deleted = false
    </select>

    <!-- 전체 카테고리 목록 조회 - 페이징 -->
    <select id="findAllPaged" resultType="org.example.board_cafe_kiosk_2603.dto.admin.product.CategoryResponseDTO">
        SELECT c.id,
               c.name,
               c.type,
               CASE
                   WHEN c.type = 'GAME' THEN
                           (SELECT COUNT(*) FROM game g WHERE g.category_id = c.id)
                   ELSE
                       (SELECT COUNT(*) FROM menu m WHERE m.category_id = c.id AND m.is_deleted = false)
                   END AS productCount
        FROM category c
        ORDER BY c.type, c.id
        LIMIT #{size} OFFSET #{skip}
    </select>

    <!-- 전체 카테고리 수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM category
    </select>

</mapper>