<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.kiosk.tableMessage.TableMessageMapper">

    <!-- 메시지 저장 -->
    <insert id="insert"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO table_message (table_id, macro_id, direction, content, is_read)
        VALUES (#{tableId}, #{macroId}, #{direction}, #{content}, FALSE)
    </insert>

    <!-- 안 읽은 메시지 전체 조회 (최신순) -->
    <select id="findUnread" resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.tableMessage.TableMessage">
        SELECT id, table_id, macro_id, direction, content, is_read, created_at
        FROM table_message
        WHERE is_read = FALSE
        ORDER BY created_at DESC
    </select>

    <!-- 테이블별 메시지 조회 (최신순) -->
    <select id="findByTableId" resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.tableMessage.TableMessage">
        SELECT id, table_id, macro_id, direction, content, is_read, created_at
        FROM table_message
        WHERE table_id = #{tableId}
        ORDER BY created_at DESC
    </select>

    <!-- 키오스크용: 관리자->테이블 미확인 메시지 -->
    <select id="findUnreadStaffByTableId" resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.tableMessage.TableMessage">
        SELECT id, table_id, macro_id, direction, content, is_read, created_at
        FROM table_message
        WHERE table_id = #{tableId}
          AND is_read = FALSE
          AND direction = 'STAFF_TO_TABLE'
        ORDER BY created_at ASC
    </select>

    <!-- 단건 읽음 처리 -->
    <update id="markAsRead">
        UPDATE table_message
        SET is_read = TRUE
        WHERE id = #{id}
    </update>

    <!-- 전체 읽음 처리 -->
    <update id="markAllAsRead">
        UPDATE table_message
        SET is_read = TRUE
        WHERE is_read = FALSE
    </update>

</mapper>
