<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.kiosk.order.OrdersMapper">

    <!-- 주문 생성 (PK 반환) -->
    <insert id="insertOrder"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (session_id, table_id, customer_phone, status, total_amount)
        VALUES (#{sessionId}, #{tableId}, #{customerPhone}, #{status}, #{totalAmount})
    </insert>

    <!-- 주문 아이템 삽입 (PK 반환) -->
    <insert id="insertOrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, menu_id, menu_name, price, quantity)
        VALUES (#{orderId}, #{menuId}, #{menuName}, #{price}, #{quantity})
    </insert>

    <!-- 주문 상태 업데이트 -->
    <update id="updateOrderStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <!-- 주문 ID로 단건 조회 -->
    <select id="findByOrderId"
            resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.order.Orders">
        SELECT id, session_id, table_id, customer_phone, status, total_amount, ordered_at
        FROM orders
        WHERE id = #{orderId}
    </select>

    <!-- 세션 ID로 주문 목록 조회 -->
    <select id="findBySessionId"
            resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.order.Orders">
        SELECT id, session_id, table_id, customer_phone, status, total_amount, ordered_at
        FROM orders
        WHERE session_id = #{sessionId}
        ORDER BY ordered_at DESC
    </select>

    <!-- 테이블 ID로 최근 주문 조회 -->
    <select id="findLatestByTableId"
            resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.order.Orders">
        SELECT id, session_id, table_id, customer_phone, status, total_amount, ordered_at
        FROM orders
        WHERE table_id = #{tableId}
        ORDER BY ordered_at DESC
        LIMIT 1
    </select>

    <!-- 상태별 주문 목록 조회 (관리자용) -->
    <select id="findByStatus"
            resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.order.Orders">
        SELECT id, session_id, table_id, customer_phone, status, total_amount, ordered_at
        FROM orders
        WHERE status = #{status}
        ORDER BY ordered_at ASC
    </select>

    <!-- 주문 ID로 아이템 목록 조회 -->
    <select id="findItemsByOrderId"
            resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.order.OrderItem">
        SELECT id, order_id, menu_id, menu_name, price, quantity
        FROM order_item
        WHERE order_id = #{orderId}
    </select>

</mapper>
