<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.kiosk.cafePackage.CafePackageMapper">

    <select id="findAllActive" resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.cafePackage.CafePackage">
        SELECT id, name, type, duration_minutes, base_price,
               extra_price_per_min, is_active, updated_at
        FROM cafe_package
        WHERE is_active = TRUE
        ORDER BY base_price ASC
    </select>

    <select id="findById" resultType="org.example.board_cafe_kiosk_2603.domain.kiosk.cafePackage.CafePackage">
        SELECT id, name, type, duration_minutes, base_price,
               extra_price_per_min, is_active, updated_at
        FROM cafe_package
        WHERE id = #{id}
    </select>

</mapper>
